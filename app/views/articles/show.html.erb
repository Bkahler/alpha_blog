<h2 align="center"><%= @article.title %></h2>

<div class="well col-xs-8 col-xs-offset-2">
  <h4 align="center" class="description">Description:</h4>
  <hr>
  <%= simple_format(@article.description) %>
  <div class="article-actions">
    <%= link_to "Edit Article", edit_article_path(@article), class: "btn btn-xs btn-primary" %>
    <%= link_to "Delete", article_path(@article), method: :delete, class: "btn btn-xs btn-danger", data: {confirm: "Are you Sure?" } %>
    <%= link_to "All Articles", articles_path, class: "btn btn-xs btn-success" %>
  </div>
</div>

